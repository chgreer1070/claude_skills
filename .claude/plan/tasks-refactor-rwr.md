# Refactoring Tasks: the-rewrite-room

**Design Spec**: [refactor-design-rwr.md](./refactor-design-rwr.md)
**Plugin**: `plugins/the-rewrite-room/`
**Score Before**: 62/100
**Score Target**: 82+/100

---

## Dependency Graph

```text
Task 1 (plugin.json name + commands interim)  ──┐
                                                 ├── Task 6 (add doc-to-skill to commands)
Task 3 (create doc-to-skill.md command)       ──┘        │
                                                          └── Task 7 (document dependencies in plugin.json)
Task 2 (hooks.json STATUS + timeout)                                    ▲
                                                                        │
Task 4 (fix broken link + hardcoded paths)  ──── Task 5 (namespace bare agents) ─┘
```

**Parallelization summary**:

- Priority 1: Task 1, Task 2 (parallel -- different files)
- Priority 2: Task 3, Task 4 (parallel -- different files, parallel with Priority 1)
- Priority 3: Task 5 (depends on Task 4), Task 6 (depends on Task 1 + Task 3)
- Priority 4: Task 7 (depends on Task 5 + Task 6)

---

## SYNC CHECKPOINT 1: Review-Reflect-Revise

**Convergence point**: Task 1 + Task 2 + Task 3 + Task 4 outputs
**Quality gates**:
- `plugin.json` has `name: "the-rewrite-room"` and 4-entry commands array
- `hooks/hooks.json` contains `DONE|BLOCKED|FAILED` vocabulary and `timeout: 30000`
- `commands/rwr/doc-to-skill.md` exists with correct frontmatter
- No `/home/ubuntulinuxqa2/` strings remain in `workflows/audit.md`, `SKILL.md`, `rewrite-room-auditor.md`
- Broken `validate.md` link replaced with direct script invocation
**Reflection questions**:
- Do the plugin.json changes allow the plugin to load?
- Are any new file conflicts introduced?
**Proceed to Priority 3 tasks after checkpoint passes.**

---

## SYNC CHECKPOINT 2: Review-Reflect-Revise

**Convergence point**: Task 5 + Task 6 outputs
**Quality gates**:
- All agent references in `rewrite-room-author.md` use `plugin:agent-name` format
- `doc-to-skill.md` entry present in plugin.json commands array (5 total entries)
- No bare agent references remain (except `doc-freshness-guardian` which is documented as global)
**Reflection questions**:
- Did Fix 6 verification reveal the correct owning plugins?
- Are the namespace prefixes consistent with actual plugin directory names?
**Proceed to Task 7 after checkpoint passes.**

---

## Context Manifest

_Generated by context-gathering agent on 2026-03-09_

### Current State of Files Being Modified

The following files contain the exact values that need to be changed:

**File: `plugins/the-rewrite-room/.claude-plugin/plugin.json`**

Current state (lines 1-36 of JSON):
- Line 2: `"name": "rwr"` — must change to `"name": "the-rewrite-room"`
- Lines 27: `"commands": []` — empty array, must be populated with 4 entries (interim), then 5 entries (final)
- Lines 20-26: `agents` array correctly lists 5 agents (rewrite-room-auditor, rewrite-room-optimizer, rewrite-room-author, rewrite-room-doc-converter, rewrite-room-cite)
- Version: 2.5.8, description present, author info present

**File: `plugins/the-rewrite-room/hooks/hooks.json`**

Current state (lines 1-16 of JSON):
- Line 2: Description field correct
- Lines 4-14: SubagentStop hook type is `"prompt"`
- Lines 9-10: The validator prompt contains **3 text occurrences** that must be changed:
  1. Line 9: `"STATUS: [COMPLETE|BLOCKED|PARTIAL]"` → must change to `"STATUS: [DONE|BLOCKED|FAILED]"`
  2. Line 9: `"1. STATUS field present with one of: COMPLETE, BLOCKED, PARTIAL"` → must change to `"1. STATUS field present with one of: DONE, BLOCKED, FAILED"`
  3. Line 9: `"STATUS value must be COMPLETE, BLOCKED, or PARTIAL."` → must change to `"STATUS value must be DONE, BLOCKED, or FAILED."`
- Line 10: `"timeout": 15` — milliseconds, MUST change to `"timeout": 30000` (15ms is insufficient for LLM prompt evaluation)

**Existing Command Files (template for doc-to-skill.md)**

All 4 existing command files follow this pattern:
- YAML frontmatter with `---` delimiters
- Fields: `description`, `argument-hint`, `agent`, `allowed-tools`
- No workflow steps in command file body (those belong in skill SKILL.md)
- Body references the agent that will be invoked
- Example (`audit.md`):
  ```
  ---
  description: "..."
  argument-hint: "<task description — what to audit or sync>"
  agent: rewrite-room-auditor
  allowed-tools: Read, Grep, Glob, Bash, Task, Write, Edit
  ---

  Route this documentation audit or sync task to the rewrite-room-auditor agent.

  Task: $ARGUMENTS
  ```

**Existing Agents (5 total)**

All located in `plugins/the-rewrite-room/agents/`:
1. rewrite-room-auditor.md
2. rewrite-room-optimizer.md
3. rewrite-room-author.md
4. rewrite-room-doc-converter.md
5. rewrite-room-cite.md

All agents contain STATUS output contracts using `DONE|BLOCKED|FAILED` (the validators use the OLD `COMPLETE|BLOCKED|PARTIAL` — this is the mismatch that Fix 3 corrects).

### File Locations and Current Structure

**Plugin Directory Structure:**
```
plugins/the-rewrite-room/
├── .claude-plugin/
│   └── plugin.json                          [FIX 1, 2, 6, 7 touch this]
├── hooks/
│   └── hooks.json                           [FIX 3, 8 touch this]
├── commands/rwr/
│   ├── audit.md                             [existing template]
│   ├── author.md                            [existing template]
│   ├── cite.md                              [existing template]
│   ├── optimize.md                          [existing template]
│   └── doc-to-skill.md                      [FIX 4 creates this]
├── agents/
│   ├── rewrite-room-auditor.md              [FIX 6, 7 touch this]
│   ├── rewrite-room-optimizer.md
│   ├── rewrite-room-author.md               [FIX 5, 6 touch this]
│   ├── rewrite-room-doc-converter.md
│   └── rewrite-room-cite.md
├── skills/the-rewrite-room/
│   └── SKILL.md                             [FIX 4, 5, 7 touch this]
└── the-rewrite-room/
    └── workflows/
        └── audit.md                         [FIX 4, 5 touch this]
```

### Critical Details for Implementation

**Command File Format Requirements (Task 3)**

The `doc-to-skill.md` command file must:
- Have YAML frontmatter with `name: doc-to-skill` (not the filename)
- Have `agent: rewrite-room-doc-converter` field
- Have `description` field mentioning the three arguments: `<docs_path>`, `<output_plugin>`, `<output_skill>`
- Have `allowed-tools` field listing permitted tools
- Match the format of existing command files (audit.md, author.md, cite.md, optimize.md)
- NOT include workflow steps (those belong in the skill that the agent loads)

**hooks.json JSON Validity**

The entire hooks.json is valid JSON. The 3 text replacements occur within a single large JSON string value (the `"prompt"` field). Replacements must preserve JSON escaping — if the original contains `\|` (escaped pipe), the replacement must use the same escaping.

**Files NOT to Touch**

- Agent output contracts in all 5 agent files (they already use correct `DONE|BLOCKED|FAILED`)
- Any file not listed in the task file's target files
- The structure of hooks.json outside the prompt string

### Hardcoded Paths and References (Tasks 5, 7)

**Current Bare Agent References (without namespace prefix):**
1. In `rewrite-room-auditor.md` table: `doc-freshness-guardian` (bare name)
2. In SKILL.md Workflow Index mermaid: `doc-freshness-guardian` (bare name, node A3)
3. In `rewrite-room-author.md` specialist table: `gitlab-docs-expert` (bare name, subagent_type field)
4. In `rewrite-room-author.md` specialist table: `documentation-expert` (bare name, subagent_type field)

**Current Hardcoded Absolute Paths (3 occurrences in 3 files):**
- `/home/ubuntulinuxqa2/.claude/agents/doc-freshness-guardian.md` in `SKILL.md` line 98
- `/home/ubuntulinuxqa2/.claude/agents/doc-freshness-guardian.md` in `workflows/audit.md` line 29
- `/home/ubuntulinuxqa2/.claude/agents/doc-freshness-guardian.md` in `rewrite-room-auditor.md` line 44

All three reference the same global user agent (not in any plugin).

**Broken File References:**
- `workflows/audit.md` line 78 references `plugins/the-rewrite-room/the-rewrite-room/workflows/validate.md` which does not exist
- Replacement: direct script invocation `uv run plugins/plugin-creator/scripts/validate_frontmatter.py <file>`

### Task Dependency Details

**Group A (Parallel — no shared files):**
- Task 1: Edits plugin.json (name + commands interim)
- Task 2: Edits hooks.json (STATUS vocab + timeout)
- Task 3: Creates new file doc-to-skill.md
- Task 4: Edits workflows/audit.md, SKILL.md, rewrite-room-auditor.md

**Group B (Parallel, depends on Group A):**
- Task 5: Edits agent files + SKILL.md (requires Task 4 to complete first due to shared files)
- Task 6: Edits plugin.json (requires Task 1 + Task 3 complete)

**Group C (Sequential after Groups A & B):**
- Task 7: Edits plugin.json or README.md (requires Task 5 + Task 6 complete)

### Verification Patterns

**JSON Validation:**
```bash
python3 -c "import json; json.load(open('file.json'))"
```

**Grep Verification (lines that must exist):**
```bash
grep '"name"' plugin.json          # Should contain: "the-rewrite-room"
grep '"timeout"' hooks.json         # Should contain: 30000
grep 'doc-to-skill' plugin.json     # Should contain: ./commands/rwr/doc-to-skill.md
grep 'agent:' commands/rwr/doc-to-skill.md  # Should contain: rewrite-room-doc-converter
```

**Grep Verification (lines that must NOT exist):**
```bash
grep '/home/ubuntulinuxqa2/' plugins/the-rewrite-room/**/*.md    # Should return nothing
grep 'validate.md' workflows/audit.md                             # Should return nothing
grep 'COMPLETE\|PARTIAL' hooks/hooks.json                         # Should return nothing
```

**Linting:**
```bash
uv run prek run --files plugins/the-rewrite-room/.claude-plugin/plugin.json
uv run prek run --files plugins/the-rewrite-room/hooks/hooks.json
uv run prek run --files plugins/the-rewrite-room/commands/rwr/doc-to-skill.md
```

---

## Task 1: Fix plugin.json Name and Commands Array (Interim)

**Status**: ❌ NOT STARTED
**Dependencies**: None
**Priority**: 1
**Complexity**: Low
**Agent**: orchestrator

**Target**: `plugins/the-rewrite-room/.claude-plugin/plugin.json`
**Issue Type**: STRUCTURE_FIX

**Acceptance Criteria**:
1. `name` field value is `"the-rewrite-room"` (not `"rwr"`)
2. `commands` array contains exactly 4 entries: `audit.md`, `author.md`, `cite.md`, `optimize.md` (all prefixed `./commands/rwr/`)
3. No other fields in plugin.json are modified
4. JSON is valid (parseable without errors)

**Required Inputs**:
- Design spec section: Fix 1, Fix 2 (interim value)
- Source files: `plugins/the-rewrite-room/.claude-plugin/plugin.json`

**Expected Outputs**:
- `plugins/the-rewrite-room/.claude-plugin/plugin.json` (modified)

**Can Parallelize With**: Task 2, Task 3, Task 4
**Reason**: Task 1 modifies `plugin.json`, Task 2 modifies `hooks.json`, Task 3 creates a new file, Task 4 modifies `workflows/audit.md` and agent files -- no shared files

**Verification Steps**:
1. Run `python3 -c "import json; json.load(open('plugins/the-rewrite-room/.claude-plugin/plugin.json'))"` -- exit code 0
2. Run `grep '"name"' plugins/the-rewrite-room/.claude-plugin/plugin.json` -- output contains `"the-rewrite-room"`
3. Run `grep -c 'commands/rwr/' plugins/the-rewrite-room/.claude-plugin/plugin.json` -- output is `4`
4. Run `uv run prek run --files plugins/the-rewrite-room/.claude-plugin/plugin.json` -- passes

---

## Task 2: Fix hooks.json STATUS Vocabulary and Timeout

**Status**: ❌ NOT STARTED
**Dependencies**: None
**Priority**: 1
**Complexity**: Low
**Agent**: orchestrator

**Target**: `plugins/the-rewrite-room/hooks/hooks.json`
**Issue Type**: STRUCTURE_FIX

**Context**: This task was merged from Fix 3 (STATUS vocabulary) and Fix 8 (hook timeout) to avoid edit conflicts -- both modify `hooks/hooks.json`.

**Acceptance Criteria**:
1. All three STATUS vocabulary occurrences in the hooks.json prompt string use `DONE|BLOCKED|FAILED` (not `COMPLETE|BLOCKED|PARTIAL`)
2. `timeout` value is `30000` (not `15`)
3. No other content in hooks.json is modified
4. JSON is valid (parseable without errors)

**Required Inputs**:
- Design spec section: Fix 3, Fix 8
- Source files: `plugins/the-rewrite-room/hooks/hooks.json`

**Expected Outputs**:
- `plugins/the-rewrite-room/hooks/hooks.json` (modified)

**Can Parallelize With**: Task 1, Task 3, Task 4
**Reason**: hooks.json is not touched by any other task

**Verification Steps**:
1. Run `python3 -c "import json; json.load(open('plugins/the-rewrite-room/hooks/hooks.json'))"` -- exit code 0
2. Run `grep -c 'COMPLETE' plugins/the-rewrite-room/hooks/hooks.json` -- output is `0`
3. Run `grep -c 'PARTIAL' plugins/the-rewrite-room/hooks/hooks.json` -- output is `0`
4. Run `grep -c 'DONE' plugins/the-rewrite-room/hooks/hooks.json` -- output is `>= 1`
5. Run `grep -c 'FAILED' plugins/the-rewrite-room/hooks/hooks.json` -- output is `>= 1`
6. Run `grep '"timeout"' plugins/the-rewrite-room/hooks/hooks.json` -- output contains `30000`

**CoVe Checks** (accuracy-risk: medium -- incorrect STATUS vocabulary breaks all agent output validation):
- Key claims to verify:
  - The design spec identifies exactly 3 text occurrences in hooks.json that need replacement
  - All 5 agent files already use `DONE|BLOCKED|FAILED` and must NOT be changed
- Verification questions:
  1. Does `grep -c 'COMPLETE\|PARTIAL' plugins/the-rewrite-room/hooks/hooks.json` return exactly 3 before the fix?
  2. Does `grep -l 'STATUS: DONE' plugins/the-rewrite-room/agents/*.md` return all 5 agent files?
- Evidence to collect:
  - Read hooks.json and count occurrences of COMPLETE and PARTIAL
  - Grep agent files for STATUS patterns to confirm they use DONE/FAILED
- Revision rule:
  - If occurrences differ from expected 3, re-read the file and identify all locations before editing

---

## Task 3: Create doc-to-skill Command File

**Status**: ❌ NOT STARTED
**Dependencies**: None
**Priority**: 2
**Complexity**: Low
**Agent**: contextual-ai-documentation-optimizer

**Target**: `plugins/the-rewrite-room/commands/rwr/doc-to-skill.md`
**Issue Type**: STRUCTURE_FIX

**Acceptance Criteria**:
1. File exists at `plugins/the-rewrite-room/commands/rwr/doc-to-skill.md`
2. YAML frontmatter contains `name: doc-to-skill`
3. YAML frontmatter contains `agent: rewrite-room-doc-converter`
4. Description mentions the three arguments: `<docs_path>`, `<output_plugin>`, `<output_skill>`
5. No workflow steps in the command file body (those belong in the skill SKILL.md)

**Required Inputs**:
- Design spec section: Fix 4
- Source files: existing command files for format reference (`plugins/the-rewrite-room/commands/rwr/audit.md`, `plugins/the-rewrite-room/commands/rwr/author.md`)

**Expected Outputs**:
- `plugins/the-rewrite-room/commands/rwr/doc-to-skill.md` (new file)

**Can Parallelize With**: Task 1, Task 2, Task 4
**Reason**: Creates a new file that no other task touches

**Verification Steps**:
1. Run `test -f plugins/the-rewrite-room/commands/rwr/doc-to-skill.md && echo "exists"` -- output is `exists`
2. Run `grep 'agent:' plugins/the-rewrite-room/commands/rwr/doc-to-skill.md` -- output contains `rewrite-room-doc-converter`
3. Run `grep 'name:' plugins/the-rewrite-room/commands/rwr/doc-to-skill.md` -- output contains `doc-to-skill`
4. Run `uv run prek run --files plugins/the-rewrite-room/commands/rwr/doc-to-skill.md` -- passes

---

## Task 4: Fix Broken Link and Hardcoded Absolute Paths

**Status**: ❌ NOT STARTED
**Dependencies**: None
**Priority**: 2
**Complexity**: Medium
**Agent**: contextual-ai-documentation-optimizer

**Target**: Multiple files (3 files modified)
**Issue Type**: STRUCTURE_FIX

**Context**: This task was merged from Fix 5 (broken validate.md link) and Fix 7 (hardcoded absolute paths) to avoid edit conflicts -- both modify `workflows/audit.md`. Fix 7 additionally modifies `SKILL.md` and `rewrite-room-auditor.md`.

**Acceptance Criteria**:
1. `workflows/audit.md` line 78 Mermaid node references `uv run plugins/plugin-creator/scripts/validate_frontmatter.py <file>` instead of the nonexistent `workflows/validate.md`
2. No string `/home/ubuntulinuxqa2/` exists in `plugins/the-rewrite-room/the-rewrite-room/workflows/audit.md`
3. No string `/home/ubuntulinuxqa2/` exists in `plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md`
4. No string `/home/ubuntulinuxqa2/` exists in `plugins/the-rewrite-room/agents/rewrite-room-auditor.md`
5. All three hardcoded path replacements use `~/.claude/agents/doc-freshness-guardian.md (global user agent -- install separately)` format
6. The Mermaid diagram in `workflows/audit.md` remains syntactically valid after the link fix

**Required Inputs**:
- Design spec section: Fix 5, Fix 7
- Source files:
  - `plugins/the-rewrite-room/the-rewrite-room/workflows/audit.md`
  - `plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md`
  - `plugins/the-rewrite-room/agents/rewrite-room-auditor.md`

**Expected Outputs**:
- `plugins/the-rewrite-room/the-rewrite-room/workflows/audit.md` (modified)
- `plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md` (modified)
- `plugins/the-rewrite-room/agents/rewrite-room-auditor.md` (modified)

**Can Parallelize With**: Task 1, Task 2, Task 3
**Reason**: No file overlap with Tasks 1-3. Task 4 modifies workflows/audit.md, SKILL.md, and rewrite-room-auditor.md -- none of which are touched by Tasks 1-3

**Verification Steps**:
1. Run `grep -r '/home/ubuntulinuxqa2/' plugins/the-rewrite-room/` -- output is empty (no matches)
2. Run `grep 'validate.md' plugins/the-rewrite-room/the-rewrite-room/workflows/audit.md` -- output is empty (no matches)
3. Run `grep -c 'validate_frontmatter.py' plugins/the-rewrite-room/the-rewrite-room/workflows/audit.md` -- output is `>= 1`
4. Run `grep -c '~/.claude/agents/doc-freshness-guardian.md' plugins/the-rewrite-room/agents/rewrite-room-auditor.md` -- output is `>= 1`
5. Run `uv run prek run --files plugins/the-rewrite-room/the-rewrite-room/workflows/audit.md plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md plugins/the-rewrite-room/agents/rewrite-room-auditor.md` -- passes

---

## Task 5: Namespace Bare Agent References

**Status**: ❌ NOT STARTED
**Dependencies**: Task 4
**Priority**: 3
**Complexity**: Medium
**Agent**: contextual-ai-documentation-optimizer

**Target**: Multiple files (3 files modified)
**Issue Type**: AGENT_OPTIMIZE

**Context**: Three agent references lack namespace prefixes. `doc-freshness-guardian` is a global user agent (cannot be namespaced -- document as external dependency). `gitlab-docs-expert` and `documentation-expert` require verification of their owning plugin before namespace can be applied. Task 4 must complete because both tasks modify `SKILL.md` and `rewrite-room-auditor.md`.

**Acceptance Criteria**:
1. `rewrite-room-author.md` specialist table `subagent_type` values for `gitlab-docs-expert` and `documentation-expert` use `plugin-name:agent-name` format (where `plugin-name` is determined by globbing)
2. `SKILL.md` Workflow Index mermaid node A3 documents `doc-freshness-guardian` as `doc-freshness-guardian (global)` or equivalent notation indicating it is not a plugin agent
3. `rewrite-room-auditor.md` table entry for `doc-freshness-guardian` includes note that it is a global user agent dependency
4. If glob verification reveals that `gitlab-docs-expert` or `documentation-expert` do not exist in any plugin, document them as missing dependencies rather than inventing a namespace

**Required Inputs**:
- Design spec section: Fix 6
- Source files:
  - `plugins/the-rewrite-room/agents/rewrite-room-author.md`
  - `plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md`
  - `plugins/the-rewrite-room/agents/rewrite-room-auditor.md`
- Discovery: glob `plugins/**/agents/gitlab-docs-expert.md` and `plugins/**/agents/documentation-expert.md`

**Expected Outputs**:
- `plugins/the-rewrite-room/agents/rewrite-room-author.md` (modified)
- `plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md` (modified)
- `plugins/the-rewrite-room/agents/rewrite-room-auditor.md` (modified)

**Can Parallelize With**: Task 6
**Reason**: Task 5 modifies agent files and SKILL.md; Task 6 modifies only plugin.json -- no shared files

**Verification Steps**:
1. Run `grep 'gitlab-docs-expert' plugins/the-rewrite-room/agents/rewrite-room-author.md` -- output contains a colon-separated namespace prefix (e.g., `gitlab-skill:gitlab-docs-expert`) OR documents it as unresolved
2. Run `grep 'documentation-expert' plugins/the-rewrite-room/agents/rewrite-room-author.md` -- output contains a colon-separated namespace prefix OR documents it as unresolved
3. Run `grep 'doc-freshness-guardian' plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md` -- output contains `(global)` or equivalent external dependency notation
4. Run `uv run prek run --files plugins/the-rewrite-room/agents/rewrite-room-author.md plugins/the-rewrite-room/skills/the-rewrite-room/SKILL.md plugins/the-rewrite-room/agents/rewrite-room-auditor.md` -- passes

**CoVe Checks** (accuracy-risk: medium -- incorrect namespace prefix causes agent routing failures):
- Key claims to verify:
  - `gitlab-docs-expert` exists in a specific plugin's agents directory
  - `documentation-expert` exists in a specific plugin's agents directory
- Verification questions:
  1. Does `find plugins/ -path '*/agents/gitlab-docs-expert.md'` return exactly one result?
  2. Does `find plugins/ -path '*/agents/documentation-expert.md'` return exactly one result?
  3. If either returns zero results, is the agent a global user agent or does it not exist at all?
- Evidence to collect:
  - Glob results for both agent names across all plugin directories
  - If found, confirm the plugin directory name matches the namespace to be used
- Revision rule:
  - If an agent is not found in any plugin, document it as "unresolved external dependency" rather than guessing a namespace

---

## Task 6: Add doc-to-skill.md to plugin.json Commands Array

**Status**: ❌ NOT STARTED
**Dependencies**: Task 1, Task 3
**Priority**: 3
**Complexity**: Low
**Agent**: orchestrator

**Target**: `plugins/the-rewrite-room/.claude-plugin/plugin.json`
**Issue Type**: STRUCTURE_FIX

**Context**: Task 1 sets the commands array to 4 entries (interim). Task 3 creates the `doc-to-skill.md` file. This task adds the 5th entry to the commands array. Depends on both because the file must exist before being referenced, and the interim array must be in place.

**Acceptance Criteria**:
1. `commands` array contains exactly 5 entries, including `"./commands/rwr/doc-to-skill.md"`
2. The file `plugins/the-rewrite-room/commands/rwr/doc-to-skill.md` exists (created by Task 3)
3. JSON is valid (parseable without errors)

**Required Inputs**:
- Design spec section: Fix 2 (final value)
- Source files: `plugins/the-rewrite-room/.claude-plugin/plugin.json` (as modified by Task 1)
- Prerequisite artifact: `plugins/the-rewrite-room/commands/rwr/doc-to-skill.md` (created by Task 3)

**Expected Outputs**:
- `plugins/the-rewrite-room/.claude-plugin/plugin.json` (modified)

**Can Parallelize With**: Task 5
**Reason**: Task 6 modifies plugin.json; Task 5 modifies agent files and SKILL.md -- no shared files

**Verification Steps**:
1. Run `python3 -c "import json; d=json.load(open('plugins/the-rewrite-room/.claude-plugin/plugin.json')); assert len(d['commands'])==5"` -- exit code 0
2. Run `grep 'doc-to-skill' plugins/the-rewrite-room/.claude-plugin/plugin.json` -- output contains `./commands/rwr/doc-to-skill.md`
3. Run `python3 -c "import json; json.load(open('plugins/the-rewrite-room/.claude-plugin/plugin.json'))"` -- exit code 0 (valid JSON)

---

## Task 7: Document External Plugin Dependencies

**Status**: ❌ NOT STARTED
**Dependencies**: Task 5, Task 6
**Priority**: 4
**Complexity**: Medium
**Agent**: contextual-ai-documentation-optimizer

**Target**: `plugins/the-rewrite-room/.claude-plugin/plugin.json`
**Issue Type**: DOC_IMPROVE

**Context**: The plugin delegates to 11+ agents across external plugins with no declared dependencies. Users who install `the-rewrite-room` without those plugins get silent routing failures. Task 5 must complete because it resolves the owning plugins for `gitlab-docs-expert` and `documentation-expert`, which are needed to produce an accurate dependency list. Task 6 must complete because both modify plugin.json.

**Acceptance Criteria**:
1. `plugin.json` contains a `dependencies` or `peerDependencies` field listing required external plugins, OR a README.md section titled "Required plugins" exists if the schema does not support a dependencies field
2. All 5 external plugin dependencies from the design spec are listed: `development-harness`, `plugin-creator`, `summarizer`, `gitlab-skill`, `prompt-optimization-claude-45`
3. Global agent dependencies (`doc-freshness-guardian` and any unresolved agents from Task 5) are documented separately with installation instructions
4. JSON remains valid if plugin.json is modified

**Required Inputs**:
- Design spec section: Fix 9
- Source files: `plugins/the-rewrite-room/.claude-plugin/plugin.json` (as modified by Task 6)
- Task 5 output: resolved namespace prefixes for `gitlab-docs-expert` and `documentation-expert`

**Expected Outputs**:
- `plugins/the-rewrite-room/.claude-plugin/plugin.json` (modified) OR `plugins/the-rewrite-room/README.md` (new or modified)

**Can Parallelize With**: None
**Reason**: Depends on Task 5 (agent resolution) and Task 6 (last plugin.json edit) -- must run after both

**Verification Steps**:
1. Run `python3 -c "import json; json.load(open('plugins/the-rewrite-room/.claude-plugin/plugin.json'))"` -- exit code 0 (valid JSON)
2. Run `grep -c 'development-harness\|plugin-creator\|summarizer\|gitlab-skill\|prompt-optimization' plugins/the-rewrite-room/.claude-plugin/plugin.json` -- output is `>= 5` (or check README.md if dependencies documented there)
3. Run `grep 'doc-freshness-guardian' plugins/the-rewrite-room/.claude-plugin/plugin.json` OR `grep 'doc-freshness-guardian' plugins/the-rewrite-room/README.md` -- output contains installation note
4. Run `uv run prek run --files plugins/the-rewrite-room/.claude-plugin/plugin.json` -- passes

**CoVe Checks** (accuracy-risk: medium -- missing dependencies cause silent routing failures for users):
- Key claims to verify:
  - The design spec lists 5 external plugin dependencies
  - Claude Code plugin.json schema supports a `dependencies` field
- Verification questions:
  1. Does `plugins/the-rewrite-room/agents/` contain references to all 5 listed external plugins?
  2. Does any existing plugin in the marketplace use a `dependencies` field in plugin.json that could serve as a format reference?
  3. Are there additional external agent references not captured in the design spec's list?
- Evidence to collect:
  - Grep all agent files for `subagent_type` values referencing external plugins
  - Glob `plugins/*/.claude-plugin/plugin.json` and grep for `dependencies` to find schema precedent
- Revision rule:
  - If no schema precedent exists for `dependencies` in plugin.json, create a README.md section instead

---

## Dependency Summary

```text
Priority 1 (No dependencies -- execute immediately, in parallel):
  Task 1: Fix plugin.json name + commands interim     [plugin.json]
  Task 2: Fix hooks.json STATUS + timeout              [hooks.json]

Priority 2 (No dependencies -- execute in parallel with Priority 1):
  Task 3: Create doc-to-skill.md command file          [new file]
  Task 4: Fix broken link + hardcoded paths            [audit.md, SKILL.md, auditor.md]

  >>> SYNC CHECKPOINT 1 <<<

Priority 3 (Dependency-gated):
  Task 5: Namespace bare agent references              [author.md, SKILL.md, auditor.md] -- depends: Task 4
  Task 6: Add doc-to-skill to commands array           [plugin.json] -- depends: Task 1, Task 3

  >>> SYNC CHECKPOINT 2 <<<

Priority 4 (Final):
  Task 7: Document external dependencies               [plugin.json or README.md] -- depends: Task 5, Task 6
```

**Maximum parallelism**: 4 tasks (Tasks 1, 2, 3, 4 can all run simultaneously)

---

## Success Metrics

**Quantitative**:
- All 9 fixes from design spec implemented (tracked as 7 merged tasks)
- 0 hardcoded absolute paths remaining in plugin
- 0 broken file references
- 5 command files registered in plugin.json commands array
- All STATUS vocabulary aligned between hooks and agents
- Hook timeout set to functional value (30000ms)

**Qualitative**:
- Plugin loads correctly (name matches directory)
- All commands discoverable via `/help` menu
- Hook validator accepts agent output contracts
- External dependencies are documented for plugin users
- Agent references use portable namespace format
