[project]
name = "daily-releases-lib"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PyGithub>=2.1.1",
    "httpx>=0.27.0",
    "typer>=0.21.0",
    "rich>=13.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
