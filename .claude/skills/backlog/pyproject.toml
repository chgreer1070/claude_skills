[project]
name = "backlog-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.0.2",
    "pygithub>=2.8.1",
    "pydantic>=2.12.3",
    "python-frontmatter>=1.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
