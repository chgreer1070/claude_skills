feature: migrate-backlog-github-rest-to-graphql-followup-1
version: '1.0'
description: ''
goal: |-
  Update plan artifact ADR-004 deviation documentation and AC5 to reflect the verified PyGithub behavior discovered during implementation
context: |-
  The P773 implementation discovered that PyGithub's graphql_query() raises GithubException directly instead of returning {"errors": [...]} as the ADR-004 spec assumed. The implementation correctly lets the exception propagate without an errors-dict translation layer. However, the plan artifact and AC5 were not updated to record this verified deviation.

  Files to update:
  - plan/architect-migrate-backlog-github-rest-to-graphql.md — add a Divergence Notes section under ADR-004 stating: graphql_query() raises GithubException on all errors; the errors-array translation table was not implemented because it is never reached
  - plan/tasks-773-migrate-backlog-github-rest-to-graphql.md — annotate AC5 to note the verified behavior: GithubException propagates directly from graphql_query(); the ADR-004 translation table is not exercised
  - plan/T1-graphql-design-migrate-backlog-github-rest-to-graphql.md — confirm this document already records the deviation; if not, add it

  The code behavior in github.py is correct and must NOT be changed. Only documentation/plan artifacts are modified.
tasks:
- id: T1
  title: Annotate ADR-004 deviation in plan artifact and update AC5 wording
  status: not-started
  agent: python3-development:python-cli-architect
  priority: 3
  complexity: low
