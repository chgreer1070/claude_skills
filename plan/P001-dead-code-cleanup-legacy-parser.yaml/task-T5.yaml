id: T5
title: Final verification — full pytest suite and symbol grep gates
status: not-started
priority: 3
complexity: medium
description: |-
  ```yaml
  task: T5
  title: Final verification — full pytest suite and all symbol grep gates
  status: complete
  started: 2026-03-05T00:00:00Z
  completed: 2026-03-05T00:01:00Z
  agent: python3-development:python-pytest-architect
  dependencies:
    - T4
  priority: 1
  complexity: low
  accuracy-risk: low
  skills:
    - fastmcp-python-tests
    - python3-development
  parallelize-with: []
  reason: "T4 must complete first. This task only runs commands and reports results — no file writes."
  handoff: "Report: output of every verification command listed. PASS or FAIL per gate. If any gate fails, identify which task produced the residue and what was missed."
  ```

  ### Context

  This task is the convergence point for the entire deletion chain. It runs all quality gates
  defined in the architecture spec against the final state of the scripts directory to confirm
  that no dead symbol survives across any of the four modified files.

  Architecture source: `plan/architect-dead-code-cleanup-legacy-parser.md` — Quality Gates section.

  ### Objective

  Confirm that all dead symbols are absent, all live behavior is intact, and linting is clean
  by running every quality gate command from the architecture spec.

  ### Required Inputs

  - `plan/architect-dead-code-cleanup-legacy-parser.md` — Quality Gates section (the authoritative
    list of verification commands)
  - The four modified files in their post-T4 state (read-only; do not modify in this task)

  ### Requirements

  1. Run each command from the Quality Gates section of the architecture spec in the order listed.
  2. Record the exit code and output of each command.
  3. For any gate that fails (non-zero exit code or unexpected output), report the failing command,
     its output, and identify which task (T1–T4) was responsible for the residue.
  4. Do not fix failures in this task — failures are reported, then escalation is the user's
     decision.

  ### Constraints

  - No file modifications in this task.
  - Do not attempt to fix any failures found — report them and stop.

  ### Expected Outputs

  - No files created or modified.
  - Handoff report with per-gate PASS/FAIL status and command output.

  ### Acceptance Criteria

  1. `uv run pytest` exits 0 — full test suite passes.
  2. `uv run ruff check plugins/python3-development/skills/implementation-manager/scripts/` exits 0.
  3. `uv run ruff format --check plugins/python3-development/skills/implementation-manager/scripts/` exits 0.
  4. `grep -r "detect_fenced_yaml" plugins/python3-development/skills/implementation-manager/scripts/` — zero matches.
  5. `grep -r "FIELD_PARSERS\|FieldParser\|_parse_line" plugins/python3-development/skills/implementation-manager/scripts/` — zero matches.
  6. `grep -r "_update_legacy_timestamp\|_legacy_field_to_yaml\|_LEGACY_INSERT_AFTER_FIELDS" plugins/python3-development/skills/implementation-manager/scripts/` — zero matches.
  7. `grep -r "_depth" plugins/python3-development/skills/implementation-manager/scripts/` — zero matches.
  8. `parse_task_content` docstring in `implementation_manager.py` contains no references to
     "legacy markdown", "fenced YAML", or "format detection".
  9. `add_timestamp_to_task` docstring in `task_status_hook.py` contains no references to
     `_update_legacy_timestamp` or "legacy markdown".

  ### Verification Steps

  Run these commands from the repository root. All must exit 0 or return zero matches:

  1. `uv run pytest plugins/python3-development/skills/implementation-manager/scripts/ -v`
  2. `uv run ruff check plugins/python3-development/skills/implementation-manager/scripts/`
  3. `uv run ruff format --check plugins/python3-development/skills/implementation-manager/scripts/`
  4. `grep -r "detect_fenced_yaml" plugins/python3-development/skills/implementation-manager/scripts/`
  5. `grep -r "FIELD_PARSERS\|FieldParser\|_parse_line" plugins/python3-development/skills/implementation-manager/scripts/`
  6. `grep -r "_update_legacy_timestamp\|_legacy_field_to_yaml\|_LEGACY_INSERT_AFTER_FIELDS" plugins/python3-development/skills/implementation-manager/scripts/`
  7. `grep -r "_depth" plugins/python3-development/skills/implementation-manager/scripts/`
  8. `grep "legacy markdown\|fenced YAML\|format detection" plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
  9. `grep "_update_legacy_timestamp\|legacy markdown" plugins/python3-development/skills/implementation-manager/scripts/task_status_hook.py`

  ### Handoff

  Report:
  - PASS or FAIL for each of the 9 gates above
  - Full command output for any FAIL gate
  - If all gates pass: "All quality gates passed. Issue #441 deletion chain is complete."
  - If any gate fails: "Gate N failed. Residue from task TX. [exact output]"

  ---

  ## Sync Checkpoint: Post-Chain Review

  **Convergence point:** T5 COMPLETE

  **Quality gates (from T5 handoff):**

  - All 9 grep symbol gates return zero matches
  - `uv run pytest` exits 0
  - `uv run ruff check` exits 0
  - `uv run ruff format --check` exits 0

  **Reflection questions:**

  - Did any task deviate from the architecture spec? If so, was the deviation recorded?
  - Are there any docstring references to dead formats that the grep gates did not catch?
  - Does the `_parse_task_directory` docstring accurately describe post-cleanup behavior?

  **Proceed to commit only after T5 reports all gates PASS.**
