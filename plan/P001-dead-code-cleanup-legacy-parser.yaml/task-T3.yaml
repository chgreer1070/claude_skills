id: T3
title: Delete FieldParser cluster and parse_task_content dead branches from
  implementation_manager.py
status: not-started
priority: 3
complexity: medium
description: |-
  ```yaml
  task: T3
  title: Delete FieldParser cluster and parse_task_content dead branches from implementation_manager.py
  status: not-started
  agent: python3-development:python-cli-architect
  dependencies:
    - T2
  priority: 1
  complexity: high
  accuracy-risk: medium
  skills:
    - python3-development
  parallelize-with: []
  reason: "T2 must complete first so detect_fenced_yaml no longer exists in task_format.py or implementation_manager.py when this task deletes the FieldParser cluster and dead branches."
  handoff: "Report: list of deleted symbols, updated parse_task_content signature, docstring update confirmation, ruff output, pytest output (all passing), all five symbol grep gates returning zero."
  ```

  ### Context

  This task was not split; it is step 3 of the serial chain. T2 deleted `detect_fenced_yaml`
  from `task_format.py` and also removed its import from `implementation_manager.py` line 41.
  This task deletes the FieldParser class hierarchy (~150 lines), removes three dead regions
  from `parse_task_content`, updates its signature, and updates affected docstrings.

  Architecture source: `plan/architect-dead-code-cleanup-legacy-parser.md` — Decisions 1, 2.
  Scope confirmation: `plan/codebase/dead-code-scope.md` — Q2, Summary Table.

  ### Objective

  Remove all dead code in `implementation_manager.py`: the FieldParser class cluster, the
  `FIELD_PARSERS` registry, `_parse_line`, `_create_empty_task_data`, the fenced YAML recovery
  block, the legacy markdown loop, and the recursion guard. The `detect_fenced_yaml` import was
  already removed in T2. Update `parse_task_content`'s signature and docstring to reflect its
  YAML-only behavior.

  ### Required Inputs

  - `plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
    — read in full before acting
  - `plan/architect-dead-code-cleanup-legacy-parser.md` — Decisions 1 and 2 for exact deletion
    boundaries (Region A, B, C of `parse_task_content`; FieldParser section boundary notes)
  - `plan/codebase/dead-code-scope.md` — Q2 confirming all 8 subclasses are exclusively used
    by the legacy loop

  ### Requirements

  1. Read `implementation_manager.py` in full before any edit.
  2. Verify that `detect_fenced_yaml` is no longer present in the import statement at
     approximately line 41 — it was already removed in T2. Do not attempt to remove it again.
     If it is still present (T2 did not complete the removal), remove it now and note the
     deviation in the handoff.
  3. Delete the `# Field Parsers (OCP: ...)` section comment and its contents:
     - `FieldParser` base class
     - `StatusParser`, `DependenciesParser`, `AgentParser`, `PriorityParser`, `ComplexityParser`,
       `StartedParser`, `CompletedParser`, `SkillsParser` (all 8 subclasses)
     - `FIELD_PARSERS` registry
     - `_create_empty_task_data` helper (only called from legacy loop)
     - `_parse_line` function
     The section comment `# Task Parsing (SRP: ...)` that precedes `_create_empty_task_data` is
     retained if it introduces surviving parsing functions; verify its position after deletion.
  4. Delete Region A from `parse_task_content`: the `if _depth > 1:` recursion guard block.
  5. Delete Region B from `parse_task_content`: the `else:` branch of `has_yaml_frontmatter`
     that contains the fenced YAML recovery call. After deletion, the `if has_yaml_frontmatter`
     block stands alone with no `else`.
  6. Delete Region C from `parse_task_content`: the legacy markdown loop block (from the
     `# Legacy markdown path` comment through the final `return tasks`).
  7. Update `parse_task_content`'s signature: remove the `_depth: int = 0` parameter.
  8. Update `parse_task_content`'s docstring: remove all references to "legacy markdown",
     "automatic format detection", "fenced YAML recovery", and `_depth`. Describe only the
     YAML frontmatter path and the YAML parse failure warning.
  9. Update `_parse_task_directory`'s docstring: replace "Files without YAML frontmatter are
     attempted with legacy parsing" with accurate post-cleanup behavior (files without YAML
     frontmatter are skipped with a warning, or similar — verify the actual code path).
  10. Review `parse_task_file`'s docstring for any references to multi-format detection and
      update if needed.
  11. Run `uv run ruff check` and `uv run ruff format --check`. Fix any issues.
  12. Run `uv run pytest` and confirm zero failures.

  ### Constraints

  - Do not modify `task_format.py` or `task_status_hook.py` in this task.
  - Do not introduce any new functions, classes, or abstractions.
  - Do not remove the YAML frontmatter branch of `parse_task_content` (the `if has_yaml_frontmatter` block with its `try`/`except`/`else`).
  - Do not remove the YAML parse failure `except` branch — this is a live defensive path.
  - Do not use `shell=True` in any subprocess call (not applicable here — no new code is written, but stated for completeness).

  ### Expected Outputs

  - Modified: `plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
    (import change, ~200+ lines deleted, signature update, docstring updates)

  ### Acceptance Criteria

  1. `parse_task_content` signature has no `_depth` parameter.
  2. `parse_task_content` docstring contains no references to "legacy markdown", "fenced YAML",
     or "format detection".
  3. `implementation_manager.py` contains no definition of `FieldParser`, `FIELD_PARSERS`,
     `_parse_line`, or `_create_empty_task_data`.
  4. `implementation_manager.py` does not import `detect_fenced_yaml` (this was already removed
     in T2; verify it remains absent).
  5. `uv run pytest` exits 0.
  6. `uv run ruff check` exits 0 on the file.

  ### Verification Steps

  1. `grep "detect_fenced_yaml" plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
     — zero matches.
  2. `grep "FIELD_PARSERS\|FieldParser\|_parse_line\|_create_empty_task_data" plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
     — zero matches.
  3. `grep "_depth" plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
     — zero matches.
  4. `grep "legacy markdown\|fenced YAML\|format detection" plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
     — zero matches (confirms docstring update).
  5. `uv run ruff check plugins/python3-development/skills/implementation-manager/scripts/implementation_manager.py`
     — zero errors.
  6. `uv run pytest plugins/python3-development/skills/implementation-manager/scripts/ -v`
     — all tests pass.

  ### CoVe Checks

  - Key claims to verify:
    - `_create_empty_task_data` is called exclusively from the legacy markdown loop (Region C)
      and has no callers in the YAML path.
    - Removing Region B leaves the `if has_yaml_frontmatter(content):` block syntactically
      complete (no dangling `else`).
    - After removing the FieldParser block, the `# Task Parsing (SRP: ...)` section comment
      at approximately line 613 still correctly introduces the remaining functions.
  - Verification questions:
    1. Does `_create_empty_task_data` appear in any call site outside Region C (the legacy loop)?
       Search for `_create_empty_task_data(` in the file.
    2. What is the exact structure of `parse_task_content` after Region B is removed — does the
       `if has_yaml_frontmatter` block have a bare `else:` that also needs to be removed, or does
       Region B constitute the entire `else` clause?
    3. Are there any other callers of `parse_task_content` that pass a `_depth` argument? A
       signature change without updating call sites causes a `TypeError`.
  - Evidence to collect:
    - `grep "_create_empty_task_data" implementation_manager.py` — confirm single call site.
    - Read the `parse_task_content` function body before editing to map Region A/B/C precisely
      in the current file.
    - `grep "parse_task_content" implementation_manager.py` — list all call sites; confirm none
      pass `_depth=`.
  - Revision rule:
    - If `_create_empty_task_data` has live callers outside Region C, do not delete it. Report
      to handoff.
    - If any call site passes `_depth=`, update those call sites before removing the parameter.

  ### Handoff

  Report:
  - List of deleted symbols (class/function names)
  - Confirmation that `parse_task_content` signature and docstring are updated
  - Output of all six verification steps
  - Output of `uv run pytest` (full suite)
  - Any deviation from the architecture spec with justification

  ---
