feature: repo-agnostic-backlog-github-skills
verified_at: "2026-03-20T16:45:00Z"
plan_path: plan/tasks-1-repo-agnostic-backlog-github-skills.md
t0_baseline_path: plan/T0-baseline-repo-agnostic-backlog-github-skills.yaml
verdict: "PASS"
criteria_count: 4
regressions: 0
newly_passing: 1
results:
  - criterion-id: AC1
    check-command: "bash -c 'count=$(grep -r \"Jamie-BitFlight/claude_skills\" --include=\"*.py\" --include=\"*.md\" plugins/development-harness .claude/skills/ | grep -v \"^plan/\" | grep -v \"^\\.claude/backlog/\" | wc -l); echo \"Count: $count\"; [ \"$count\" -eq 0 ]'"
    t0-exit-code: 1
    tn-exit-code: 1
    status: pre-existing-fail
    stdout-diff-summary: "T0: Count 102, TN: Count 86 — hardcoded references reduced but not eliminated (16 removed, 86 remain)"
  - criterion-id: AC2
    check-command: "uv run python -c 'from backlog_core.models import discover_repo; print(discover_repo())'"
    t0-exit-code: 1
    tn-exit-code: 0
    status: newly-passing
    stdout-diff-summary: "Was FAILING (ImportError), now PASSING — discover_repo() returns 'Jamie-BitFlight/claude_skills'"
  - criterion-id: AC3
    check-command: "uv run pytest plugins/development-harness/tests/ -x -q 2>&1 | tail -5"
    t0-exit-code: 1
    tn-exit-code: 1
    status: pre-existing-fail
    stdout-diff-summary: "Was FAILING with 'dispatch_helper' missing at T0, still FAILING with same error at TN — pre-existing test infrastructure issue, not a regression from this feature"
  - criterion-id: AC4
    check-command: "uv run python -c 'from backlog_core.models import BacklogManager; m = BacklogManager(); print(len(m.list_items()))'"
    t0-exit-code: 1
    tn-exit-code: 1
    status: pre-existing-fail
    stdout-diff-summary: "Still failing — BacklogManager not importable from backlog_core.models (pre-existing issue, unchanged)"
