feature: migrate-backlog-github-rest-to-graphql-followup-2
version: '1.0'
description: ''
goal: |-
  Replace type: ignore[assignment] suppression in _resolve_labels_graphql with a properly typed dict annotation
context: |-
  File: .claude/skills/backlog/backlog_core/github.py line 114

  Current code:
      node: _LabelNode | None = repo_node.get(alias)  # type: ignore[assignment]

  Root cause: _GraphQLRepositoryData is defined as TypedDict(total=False) with no declared fields, so mypy cannot infer the value type of .get() on it.

  Fix: Change the type annotation of repo_node (line 110) from _GraphQLRepositoryData to dict[str, _LabelNode | None]. Remove the # type: ignore[assignment] comment.

  The _GraphQLRepositoryData TypedDict class can be removed or repurposed once repo_node is typed directly as dict[str, _LabelNode | None].

  After the fix:
  - uv run ruff check .claude/skills/backlog/backlog_core/github.py must exit 0
  - uv run pytest .claude/skills/backlog/tests/ -x must exit 0
  - No type: ignore comments should remain in the function
tasks:
- id: T1
  title: 'Fix type: ignore[assignment] in _resolve_labels_graphql by typing repo_node
    as dict[str, _LabelNode | None]'
  status: not-started
  agent: python3-development:python-cli-architect
  priority: 4
  complexity: low
