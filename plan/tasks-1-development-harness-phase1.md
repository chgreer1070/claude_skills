---
plan_number: 1
slug: development-harness-phase1
goal: 'Complete Phase 1 of the Development Harness Architecture Refactor: rename plugin
  namespace to dh, sync agents from python3-development, produce the SDLC stage naming
  taxonomy, audit and fix naming compliance across manifests, and write the executable
  PoC validation guide'
issue: 581
feature: Development Harness Phase 1 Completion
status: not-started
context: |
  Generated by context-gathering agent on 2026-03-18

  ## Overview and Project Structure

  The development-harness plugin (directory: `plugins/development-harness/`) implements a 7-stage SAM (Stateless Agent Methodology) pipeline that orchestrates feature development. Phase 1 completes five deliverables: (0) namespace rename from "development-harness" to "dh" in plugin.json and all references, (1) SDLC stage naming taxonomy document ratifying 7 stages against IEEE 12207/ISO 15288/SAFe, (2) naming audit applying taxonomy to manifests with testing-verification → testing-final-verification rename, (3) executable PoC validation guide for end-to-end dispatch, (4) agent sync from python3-development with dh namespace applied.

  **Critical invariant**: The directory `plugins/development-harness/` NEVER changes name. Only the plugin namespace (JSON name field and all /development-harness: and @development-harness: text references) changes to dh.

  ### Key External Dependencies

  - Architecture spec: `plan/architect-development-harness-phase1.md` (Deliverable 1-3 sections contain exact content requirements, tables, mappings, naming rules)
  - Codebase analysis: `plan/codebase/DEVELOPMENT-HARNESS-ARCHITECTURE.md` (7-Stage Pipeline, Layer Model, Manifest System sections)
  - Language manifests in `plugins/python3-development/manifests/`:
    - `python3/language-manifest.yaml` (T02: rename testing-verification key to testing-final-verification)
    - `python3-cli/language-manifest.yaml` (T02: verify compliance, no changes expected)
  - Workflow skill directories: `plugins/development-harness/skills/workflows/` (7 directories provide Layer 1 bare stage names: discovery, planning, context-integration, task-decomposition, execution, forensic-review, final-verification)
  - Utility scripts (T03 reads these to extract actual CLI arguments):
    - `plugins/development-harness/scripts/manifest_discovery.py`
    - `plugins/development-harness/scripts/manifest_resolver.py`
    - `plugins/development-harness/scripts/dispatch_helper.py`

  ## Task Execution Flow

  **Dependency sequence**: T00 → {T01 parallel T04} → T02 → T03

  ### T00: Namespace Rename (Priority 1)
  - Agent: general-purpose
  - Scope: Text-level replacement of `development-harness:` with `dh:` across plugin files
  - Files: plugin.json (name field), CLAUDE.md, all SKILL.md, all agent files, marketplace.json
  - Constraints: No directory renames; preserve relative paths; preserve filesystem path references

  ### T01: SDLC Stage Naming Taxonomy (Priority 2, parallel with T04)
  - Agent: rwr:rewrite-room-author (skill: dh:clear-cove-task-design)
  - Creates: `plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md` (NEW)
  - 4 sections: (1) Layer 1 bare stage names (7 entries with stage_id, name, purpose, inputs, outputs, artifact_path), (2) Layer 2 domain-prefixed naming pattern (5 closed domains: planning/design/implementation/testing/review) + bare-vs-prefixed rules, (3) IEEE 12207/ISO 15288/SAFe mappings for all 7 stages, (4) 8 numbered naming rules with examples
  - Secondary: Updates `language-manifest-schema.md` with reference line to new taxonomy doc
  - Source: Architecture spec Deliverable 1 contains all required content and structures

  ### T02: Naming Audit and Manifest Updates (Priority 3, depends on T01)
  - Agent: general-purpose
  - File changes (all 4):
    1. `plugins/python3-development/manifests/python3/language-manifest.yaml`: Rename `testing-verification` → `testing-final-verification` (value unchanged)
    2. `plugins/python3-development/manifests/python3-cli/language-manifest.yaml`: Read & verify (no changes expected)
    3. `language-manifest-schema.md`: Add Stage Skills section with YAML example (using updated testing-final-verification key) and link to taxonomy doc
    4. `generic-stage-agent.md`: Add taxonomy Layer 1/Layer 2 references in input 2 and 3 descriptions
  - Constraints: YAML preservation (indentation, quoting); single key rename only; no structural changes to agent file

  ### T03: PoC Validation Guide (Priority 4, depends on T02)
  - Agent: rwr:rewrite-room-author (skill: dh:clear-cove-task-design)
  - Creates: `plugins/development-harness/docs/poc-validation-guide.md` (NEW)
  - 6 sections: Prerequisites, Discover manifest, Resolve roles, Build dispatch prompt, Dispatch agent, Expected output
  - CoVe gate: MUST read 3 scripts (manifest_discovery.py, manifest_resolver.py, dispatch_helper.py) to extract actual CLI argument names and verify commands match
  - Critical: All expected JSON output examples must use testing-final-verification (from T02 output), not old testing-verification key
  - Source: Architecture spec Deliverable 3 provides command templates and expected output structures

  ### T04: Agent Sync from python3-development (Priority 2, parallel with T01)
  - Agent: general-purpose
  - Strategy: COPY-THEN-PATCH (copy p3d version first as baseline, then apply dh namespace, then preserve any dh-specific content)
  - 10 agents to sync (copy from p3d with namespace adaptation):
    1-10: feature-researcher, codebase-analyzer, context-gathering, context-refinement, plan-validator, feature-verifier, integration-checker, doc-drift-auditor, ecosystem-researcher, swarm-task-planner
  - 2 agents to leave unchanged (dh-only): generic-stage-agent.md, service-docs-maintainer.md
  - Namespace replacement: Text-level sed-style (python3-development: → dh:) on all three patterns
  - Constraints: No new files; read-only on python3-development/; preserve filesystem paths

  ## Key Terminology

  **Layer 1 (Bare Stage Names)**: Workflow skill directory names used as input 2 to generic-stage-agent:
  - discovery, planning, context-integration, task-decomposition, execution, forensic-review, final-verification (7 total)

  **Layer 2 (Domain-Prefixed Names)**: Manifest stage_skills keys using {domain}-{sdlc-stage} pattern, passed as input 3:
  - Closed domain list: planning, design, implementation, testing, review (5 domains)
  - Example keys: discovery (bare stage), design (bare domain), planning-context-integration (domain-prefixed)

  **Naming invariants**:
  - Directory: `plugins/development-harness/` stays unchanged
  - Plugin name (JSON): "development-harness" → "dh"
  - Skill paths: /development-harness: → /dh:
  - Agent refs: @development-harness: → @dh:

  ## File Reference Map

  | Path | Modified | Purpose | Scope |
  |------|----------|---------|-------|
  | plan/architect-development-harness-phase1.md | — | Architecture spec with Deliverable 1-3 sections | Read-only |
  | plan/codebase/DEVELOPMENT-HARNESS-ARCHITECTURE.md | — | Codebase analysis | Read-only |
  | plugins/development-harness/.claude-plugin/plugin.json | T00 | Plugin name field | Change: name → "dh" |
  | .claude-plugin/marketplace.json | T00 | Plugin registry | Change: plugin entry name → "dh" |
  | plugins/development-harness/CLAUDE.md | T00 | Developer guide | Replace: /development-harness: → /dh: |
  | plugins/development-harness/skills/*/SKILL.md | T00 | Skill definitions | Replace: skill/agent refs |
  | plugins/development-harness/agents/*.md | T00, T04 | Agent files | Replace namespace; T04 syncs 10 from p3d |
  | plugins/python3-development/manifests/python3/language-manifest.yaml | T02 | Language manifest | Rename: testing-verification → testing-final-verification |
  | plugins/python3-development/manifests/python3-cli/language-manifest.yaml | T02 | Language manifest | Read & verify |
  | plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md | T01 | NEW reference doc | Create: 4 sections (stages, domains, mappings, rules) |
  | plugins/development-harness/skills/development-harness/references/language-manifest-schema.md | T02 | Reference doc | Add: Stage Skills section with example |
  | plugins/development-harness/docs/poc-validation-guide.md | T03 | NEW guide doc | Create: 6-section executable walkthrough |
  | plugins/development-harness/scripts/manifest_discovery.py | — | Utility script | Read for CLI args (T03 CoVe gate) |
  | plugins/development-harness/scripts/manifest_resolver.py | — | Utility script | Read for CLI args (T03 CoVe gate) |
  | plugins/development-harness/scripts/dispatch_helper.py | — | Utility script | Read for CLI args (T03 CoVe gate) |

  ## Implementation Standards

  **Markdown (T01, T03)**: All code fences must have language specifiers (```yaml, ```bash, ```python). All file references use markdown syntax [text](./relative/path.md) with ./ prefix. Skill/agent refs use /dh: and @dh: namespace (post-T00).

  **YAML (T02, T04)**: Preserve indentation and quoting style. Single key rename in manifest; no structural changes.

  **Quality gates**:
  - T00 AC1: grep returns zero `development-harness:` namespace refs (excluding directory components)
  - T01 AC2: Taxonomy file complete with 7 stages, 5 domains, IEEE/ISO/SAFe mappings, 8 rules
  - T02 AC3: testing-verification renamed to testing-final-verification; python3-cli verified compliant
  - T03 AC6: All CLI commands verified against actual script definitions; expected output uses correct key
  - T04 AC8: Synced agents reflect p3d baseline with dh: namespace; generic-stage-agent unchanged

  ## GitHub and Tracking

  - Issue: #581
  - Feature slug: development-harness-phase1
  - Task file: plan/tasks-1-development-harness-phase1.md
acceptance_criteria:
- 'AC1: plugin.json has name dh; zero grep hits for development-harness: (colon suffix)
  in plugin files excluding directory paths and this task file'
- 'AC2: Taxonomy document exists at plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md
  with all 4 sections (Layer 1 stage names, Layer 2 domain-prefixed names, IEEE/ISO/SAFe
  mappings, naming rules)'
- 'AC3: All stage_skills keys in plugins/python3-development/manifests/python3/language-manifest.yaml
  follow the {domain}-{sdlc-stage} pattern -- testing-verification renamed to testing-final-verification'
- 'AC4: language-manifest-schema.md contains a Stage Skills section referencing the
  taxonomy document'
- 'AC5: generic-stage-agent.md inputs 2 and 3 contain taxonomy layer references'
- 'AC6: PoC validation guide exists at plugins/development-harness/docs/poc-validation-guide.md
  with runnable uv run commands for manifest discovery, role resolution, and dispatch'
- 'AC7: All new markdown files have language specifiers on code fences and use [text](./path)
  link syntax'
- 'AC8: Each dh agent copied from python3-development reflects the p3d version as
  baseline with dh namespace applied; generic-stage-agent is unchanged'
tasks:
- task: T00
  title: Namespace rename -- change plugin name from development-harness to dh
  status: complete
  agent: general-purpose
  dependencies: []
  priority: 1
  complexity: low
  accuracy-risk: low
  skills: []
  parallelize-with: []
  reason: All other tasks reference the dh namespace -- this must land first
  handoff: plugin.json updated, all internal references changed. T01 and T04 can
    proceed.
  body: |
    ## Context

    The development-harness plugin is being renamed from namespace "development-harness" to "dh"
    for brevity. This changes invocation paths from /development-harness:skill-name to /dh:skill-name
    and @development-harness:agent-name to @dh:agent-name.

    CRITICAL: The directory plugins/development-harness/ does NOT change. Only the "name" field
    in plugin.json and internal text references change. Previous sessions kept reverting this rename
    because they confused directory name with plugin name. The directory name is a filesystem path.
    The plugin name (in plugin.json "name" field) is the namespace used for invocation. These are
    independent.

    ## Objective

    Change the plugin namespace from "development-harness" to "dh" across all plugin files while
    leaving the directory structure unchanged.

    ## Inputs

    - plugins/development-harness/.claude-plugin/plugin.json (current name: "development-harness")
    - plugins/development-harness/CLAUDE.md
    - All files under plugins/development-harness/skills/*/SKILL.md
    - All files under plugins/development-harness/agents/*.md
    - .claude-plugin/marketplace.json (line 37: "name": "development-harness")

    ## Requirements

    ### plugin.json

    1. Change "name" field from "development-harness" to "dh"
    2. Do NOT change "description", "version", "author", or any other field
    3. Do NOT rename or move the file or its parent directory

    ### CLAUDE.md

    4. Update "Name: development-harness" to "Name: dh"
    5. Replace all /development-harness: skill references with /dh: references
    6. Replace all @development-harness: agent references with @dh: references
    7. Do NOT change relative file path references (./skills/..., ./agents/...) -- those are
       filesystem paths, not namespace references

    ### SKILL.md files

    8. In every SKILL.md under plugins/development-harness/skills/, replace /development-harness:
       references with /dh: references
    9. Replace @development-harness: references with @dh: references
    10. Do NOT change directory paths or relative file references

    ### Agent files

    11. In every .md file under plugins/development-harness/agents/, replace /development-harness:
        references with /dh: references
    12. Replace @development-harness: references with @dh: references
    13. Do NOT change directory paths or relative file references

    ### marketplace.json

    14. In .claude-plugin/marketplace.json, update the "name" field for this plugin from
        "development-harness" to "dh"
    15. Do NOT change the "source" field -- it correctly points to "./plugins/development-harness"
        (filesystem path, not namespace)

    ## Constraints

    - Do NOT rename or move any directories or files
    - Do NOT change filesystem path references (./plugins/development-harness/..., ./skills/..., etc.)
    - Do NOT change references in files outside plugins/development-harness/ and .claude-plugin/marketplace.json
    - Do NOT manually edit version fields in plugin.json or marketplace.json -- the pre-commit hook handles versioning

    ## Expected Outputs

    - plugins/development-harness/.claude-plugin/plugin.json (modified)
    - plugins/development-harness/CLAUDE.md (modified)
    - All plugins/development-harness/skills/*/SKILL.md files with namespace references (modified)
    - All plugins/development-harness/agents/*.md files with namespace references (modified)
    - .claude-plugin/marketplace.json (modified)

    ## Acceptance Criteria

    1. plugins/development-harness/.claude-plugin/plugin.json contains "name": "dh"
    2. grep -r "development-harness:" plugins/development-harness/ returns zero matches
       (excluding directory path components in the grep output itself)
    3. grep "/development-harness:" .claude-plugin/marketplace.json returns zero matches
    4. .claude-plugin/marketplace.json contains "name": "dh" for this plugin entry
    5. All /dh: and @dh: references in modified files point to valid skill/agent names
    6. No directories or files were renamed or moved

    ## Verification Steps

    1. Read plugins/development-harness/.claude-plugin/plugin.json and confirm "name": "dh"
    2. Run: grep -rn "development-harness:" plugins/development-harness/ --include="*.md" --include="*.json"
       Filter out lines where "development-harness" appears only as a directory path component.
       Must return zero namespace reference matches.
    3. Run: grep -n '"name": "development-harness"' .claude-plugin/marketplace.json
       Must return zero results.
    4. Run: grep -n '"name": "dh"' .claude-plugin/marketplace.json
       Must return exactly one result.
    5. Verify no files were renamed: ls plugins/development-harness/.claude-plugin/plugin.json
       (path must still exist)
    6. Run: uv run prek run --files plugins/development-harness/CLAUDE.md
  started: '2026-03-18T21:55:16.383513+00:00'
- task: T01
  title: Write SDLC stage naming taxonomy document
  status: not-started
  agent: rwr:rewrite-room-author
  dependencies:
  - T00
  priority: 2
  complexity: medium
  accuracy-risk: medium
  skills:
  - dh:clear-cove-task-design
  parallelize-with:
  - T04
  reason: 'Foundation document -- T02 and T03 both reference this taxonomy. Depends
    on T00 because skill activation paths use dh: namespace.'
  handoff: File written at
    plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md.
    Also updated language-manifest-schema.md with reference line. T02 can
    proceed.
  body: |
    ## Context

    The development-harness plugin (namespace: dh after T00) implements a 7-stage SAM pipeline
    with workflow skills under plugins/development-harness/skills/workflows/. Language manifests
    (YAML files in plugins/python3-development/manifests/) use a {domain}-{sdlc-stage} naming
    convention for stage_skills keys. No document currently ratifies the stage names or defines
    this convention formally.

    This task was scoped by the product owner. The resolved design decisions are:
    - Two-layer naming is INTENTIONAL (bare stage names for cross-cutting skills, domain-prefixed for manifest keys)
    - Domain prefixes are a CLOSED LIST: planning, design, implementation, testing, review
    - The stage name is final-verification (not verification) -- testing-verification is non-compliant

    Architecture spec: plan/architect-development-harness-phase1.md (Deliverable 1 section)
    Codebase analysis: plan/codebase/DEVELOPMENT-HARNESS-ARCHITECTURE.md (Stage Naming Convention + Language Manifest System sections)

    ## Objective

    Produce a single reference document that maps SAM stage names to IEEE 12207 / ISO 15288 / SAFe
    terminology, formally defines the {domain}-{sdlc-stage} naming convention with closed domain
    list and usage rules. Also add a reference line to language-manifest-schema.md pointing to this
    new taxonomy doc.

    ## Inputs

    - Architecture spec Deliverable 1 section: plan/architect-development-harness-phase1.md
      (contains exact section structure, field definitions, 7-stage table, domain list table,
      example validation table, and 8 naming rules)
    - Codebase analysis: plan/codebase/DEVELOPMENT-HARNESS-ARCHITECTURE.md
      (7-Stage Pipeline section for stage purposes/inputs/outputs, Language Manifest System
      section for stage_skills format)
    - Existing workflow skill directories for skill_activation paths:
      plugins/development-harness/skills/workflows/ (7 subdirectories)
    - Existing default development flow reference:
      plugins/development-harness/skills/development-harness/references/default-development-flow.md
    - language-manifest-schema.md to update with reference:
      plugins/development-harness/skills/development-harness/references/language-manifest-schema.md

    ## Requirements

    ### Section 1: Layer 1 -- Cross-Cutting Stage Names

    1. List all 7 bare stage names as used in workflow skill directory names
    2. Each entry must include: stage_id (S1-S7), name, skill_activation path
       (/dh:{name}), purpose (one sentence), inputs, outputs, artifact_path
    3. Use the exact stage data from the architecture spec Deliverable 1 Section 1 table
    4. Include a brief paragraph explaining these are the names passed as input 2
       (cross-cutting stage skill) to the generic stage agent

    ### Section 2: Layer 2 -- Domain-Prefixed Skill Names

    5. Define the {domain}-{sdlc-stage} pattern with the closed domain list:
       planning, design, implementation, testing, review
    6. Include a table showing each domain, which stages it covers, and rationale
       (use the exact table from architecture spec Deliverable 1 Section 2)
    7. Include a validation table showing each python3 manifest key, its domain,
       SDLC stage, and compliance status. The testing-verification key must show as
       NON-COMPLIANT with note "should be testing-final-verification"
    8. Document the 3 bare-vs-prefixed usage rules from the architecture spec
    9. Document the rule for adding new domains (requires taxonomy update + schema validation update)

    ### Section 3: IEEE 12207 / ISO 15288 / SAFe Mappings

    10. Map all 7 stages to their closest equivalents in IEEE 12207, ISO 15288, and SAFe
    11. Use the exact mapping table from architecture spec Deliverable 1 Section 3
    12. Include the caveat note about approximate mappings (composite activities vs formal processes)

    ### Section 4: Naming Convention Rules

    13. List all 8 numbered rules from architecture spec Deliverable 1 Section 4
    14. Each rule must be actionable (a consumer can determine compliance by reading the rule)
    15. Include at least 2 examples per rule where the rule text supports it

    ### language-manifest-schema.md update

    16. Add a reference line to plugins/development-harness/skills/development-harness/references/language-manifest-schema.md
        pointing to the new taxonomy document. Use markdown link syntax:
        [SDLC Stage Naming Taxonomy](./sdlc-stage-taxonomy.md)
    17. Place the reference in an appropriate location (Sources/References section if it exists,
        or add a brief "See also" note near the top of the file)

    ## Constraints

    - Do NOT invent stage names or domains not present in the architecture spec or codebase
    - Do NOT include abbreviation exceptions -- the product owner decided strict compliance
    - All code fences must have language specifiers
    - All file references must use [text](./relative/path) markdown link syntax
    - Relative paths are relative to the file being written (it lives in references/ alongside
      other reference docs)
    - Skill activation paths must use /dh: namespace (not /development-harness:)
    - Use testing-final-verification (NOT testing-verification) in any examples showing
      the corrected naming

    ## Expected Outputs

    - plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md (new)
    - plugins/development-harness/skills/development-harness/references/language-manifest-schema.md (modified -- reference line added)

    ## Acceptance Criteria

    1. Taxonomy file exists at the specified path
    2. Section 1 contains all 7 stages with stage_id, name, skill_activation, purpose, inputs,
       outputs, artifact_path fields
    3. Section 2 contains the closed domain list table with all 5 domains and coverage rationale
    4. Section 2 includes the {domain}-{sdlc-stage} pattern definition and bare-vs-prefixed rules
    5. Section 3 maps all 7 stages to IEEE 12207, ISO 15288, and SAFe with clause/section numbers
    6. Section 4 lists at least 8 numbered naming rules with examples
    7. No abbreviation exceptions documented (strict compliance only)
    8. All code fences have language specifiers
    9. All file references use markdown link syntax with ./ prefix
    10. Skill activation paths use /dh: namespace throughout
    11. testing-final-verification used (NOT testing-verification) in any examples
    12. language-manifest-schema.md contains a reference line pointing to sdlc-stage-taxonomy.md

    ## Verification Steps

    1. Read the taxonomy file and confirm all 4 sections exist with required content
    2. Count stage entries in Section 1 -- must be exactly 7
    3. Count domains in Section 2 closed list -- must be exactly 5
       (planning, design, implementation, testing, review)
    4. Count mapping rows in Section 3 -- must be exactly 7
    5. Count rules in Section 4 -- must be at least 8
    6. Grep for code fences without language specifiers:
       grep -Pn '^```$' plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md
       (must return no results)
    7. Grep for old namespace:
       grep "development-harness:" plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md
       (must return no results)
    8. Grep for taxonomy reference in schema doc:
       grep "sdlc-stage-taxonomy" plugins/development-harness/skills/development-harness/references/language-manifest-schema.md
       (must return at least 1 result)
    9. Run linting:
       uv run prek run --files plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md
       uv run prek run --files plugins/development-harness/skills/development-harness/references/language-manifest-schema.md

    ## CoVe Checks

    - Key claims to verify:
      - IEEE 12207 clause numbers (6.4.1, 6.4.2, etc.) match the actual standard structure
      - ISO 15288 clause numbers match the actual standard structure
      - SAFe practice names are real SAFe terminology
    - Verification questions:
      1. Does IEEE 12207:2017 Section 6.4.1 actually cover "Stakeholder Needs Definition"?
      2. Does ISO 15288:2023 Section 6.4.1 actually cover "Stakeholder Needs and Requirements Definition"?
      3. Are "Explore / Define", "Plan / PI Planning", etc. actual SAFe practice names?
    - Evidence to collect:
      - Architecture spec provides the mapping table with clause numbers -- use those as the
        primary source but note in the document that mappings are approximate
    - Revision rule:
      - If any clause number cannot be verified against available sources, add "(approximate)"
        qualifier to that specific mapping cell

    ## Coordination Note

    Coordination note: This task also adds a reference line to language-manifest-schema.md.
    Add the reference to the taxonomy document at the END of the file in a new "## See Also"
    or "## References" section. Do not insert inline. T02 will add a "Stage Skills" section
    above this — writing to the end ensures no overlap.
- task: T02
  title: Naming audit -- update manifests and agent references for taxonomy
    compliance
  status: not-started
  agent: general-purpose
  dependencies:
  - T01
  priority: 3
  complexity: low
  accuracy-risk: low
  skills: []
  parallelize-with: []
  reason: Depends on taxonomy document (T01) for authoritative naming rules
  handoff: All 3 files updated. T03 can proceed with accurate manifest key names
    in its examples.
  body: |
    ## Context

    T01 produced the SDLC stage naming taxonomy at
    plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md.
    T01 also added a reference line to language-manifest-schema.md. The product owner resolved
    that testing-verification must be renamed to testing-final-verification (strict
    {domain}-{sdlc-stage} compliance).

    This task applies the taxonomy to 3 files: renaming the non-compliant key in the python3
    manifest, verifying the python3-cli manifest, and adding taxonomy layer references to the
    generic stage agent. The language-manifest-schema.md Stage Skills section addition is also
    handled here.

    Architecture spec: plan/architect-development-harness-phase1.md (Deliverable 2 section)

    ## Objective

    All manifest stage_skills keys comply with the taxonomy naming convention, the schema doc
    has a Stage Skills section, and the generic stage agent references taxonomy layers.

    ## Inputs

    - Architecture spec Deliverable 2: plan/architect-development-harness-phase1.md
    - Taxonomy document (T01 output):
      plugins/development-harness/skills/development-harness/references/sdlc-stage-taxonomy.md
    - Files to update:
      - plugins/python3-development/manifests/python3/language-manifest.yaml
      - plugins/python3-development/manifests/python3-cli/language-manifest.yaml
      - plugins/development-harness/skills/development-harness/references/language-manifest-schema.md
      - plugins/development-harness/agents/generic-stage-agent.md

    ## Requirements

    ### python3 language-manifest.yaml

    1. Rename the stage_skills key `testing-verification` to `testing-final-verification`
    2. The value (`[python3-testing]`) remains unchanged
    3. All other keys remain unchanged -- they are already compliant per the architecture spec audit

    ### python3-cli language-manifest.yaml

    4. Read the file and verify all stage_skills keys comply with the taxonomy
    5. Per the architecture spec audit, the keys (design, implementation) are compliant
       override keys matching the base manifest. No changes expected, but verify.

    ### language-manifest-schema.md

    6. Add a new section titled "Stage Skills (Optional)" in an appropriate location
       (between the existing "Project Detection" and any existing Conventions section, or at the end
       of the schema sections if no Conventions section exists)
    7. The new section must:
       a. Link to the taxonomy document: [SDLC Stage Naming Taxonomy](./sdlc-stage-taxonomy.md)
       b. Define the stage_skills YAML format (map of string keys to string arrays)
       c. State that keys must follow the naming convention defined in the taxonomy
       d. Include the python3 manifest's stage_skills block as a worked example
          (use the UPDATED block with testing-final-verification, not the old key)
    8. Add a Sources/References entry linking to the taxonomy document if a references
       section exists

    ### generic-stage-agent.md

    9. In the inputs section, find where input 2 (Cross-Cutting Stage Skill) is described
       and add a parenthetical: "(bare stage name from the SDLC taxonomy Layer 1)"
    10. In the inputs section, find where input 3 (Domain Skills) is described and add a
        parenthetical: "(domain-prefixed names from the SDLC taxonomy Layer 2, resolved
        via manifest stage_skills)"
    11. No other structural changes to the agent file

    ## Constraints

    - Do NOT modify any values in manifest files -- only rename the one key specified
    - Do NOT restructure the generic-stage-agent.md -- add parenthetical references only
    - Do NOT modify dispatch_helper.py or any Python scripts in this task
    - Preserve existing YAML formatting (indentation, quoting style) in manifest files
    - All code fences in language-manifest-schema.md additions must have language specifiers
    - All file references must use [text](./path) markdown link syntax

    ## Expected Outputs

    - plugins/python3-development/manifests/python3/language-manifest.yaml (modified)
    - plugins/python3-development/manifests/python3-cli/language-manifest.yaml (verified, possibly unchanged)
    - plugins/development-harness/skills/development-harness/references/language-manifest-schema.md (modified)
    - plugins/development-harness/agents/generic-stage-agent.md (modified)

    ## Acceptance Criteria

    1. The key `testing-verification` no longer exists in
       plugins/python3-development/manifests/python3/language-manifest.yaml
    2. The key `testing-final-verification` exists with value `[python3-testing]` in that file
    3. All other stage_skills keys in both manifest files are unchanged
    4. language-manifest-schema.md contains a "Stage Skills" section with a markdown link
       to sdlc-stage-taxonomy.md
    5. The Stage Skills section includes a YAML code example showing the updated python3
       stage_skills (with testing-final-verification)
    6. generic-stage-agent.md input 2 description contains "Layer 1" reference
    7. generic-stage-agent.md input 3 description contains "Layer 2" reference
    8. No python scripts were modified

    ## Verification Steps

    1. Grep for old key name:
       grep -r "testing-verification" plugins/python3-development/manifests/
       (must return zero results -- the old exact key must not appear)
    2. Grep for new key name:
       grep "testing-final-verification" plugins/python3-development/manifests/python3/language-manifest.yaml
       (must return exactly 1 result)
    3. Verify YAML is valid:
       uv run python -c "import yaml; yaml.safe_load(open('plugins/python3-development/manifests/python3/language-manifest.yaml'))"
    4. Grep for taxonomy link in schema doc:
       grep "sdlc-stage-taxonomy" plugins/development-harness/skills/development-harness/references/language-manifest-schema.md
       (must return at least 1 result)
    5. Grep for Layer references in agent doc:
       grep -c "Layer [12]" plugins/development-harness/agents/generic-stage-agent.md
       (must return at least 2)
    6. Confirm no Python files changed:
       git diff --name-only -- "*.py" | wc -l
       (must return 0)
    7. Run linting on modified markdown files:
       uv run prek run --files plugins/development-harness/skills/development-harness/references/language-manifest-schema.md
       uv run prek run --files plugins/development-harness/agents/generic-stage-agent.md

    ## Coordination Note

    Coordination note: T01 has already added a reference line at the END of language-manifest-schema.md.
    Add the "Stage Skills (Optional)" section BEFORE the final "## See Also" / "## References"
    section added by T01 — i.e., insert it at the end of the schema sections, not at the
    absolute end of the file.
- task: T03
  title: Write executable PoC validation guide
  status: not-started
  agent: rwr:rewrite-room-author
  dependencies:
  - T02
  priority: 4
  complexity: medium
  accuracy-risk: medium
  skills:
  - dh:clear-cove-task-design
  parallelize-with: []
  reason: Depends on T02 because the guide's expected output must reflect the
    renamed testing-final-verification key
  handoff: File written at
    plugins/development-harness/docs/poc-validation-guide.md. All Phase 1
    deliverables complete.
  body: |
    ## Context

    The development-harness plugin (namespace: dh) has manifest discovery, resolution, and dispatch
    scripts but no documented end-to-end validation procedure. A contributor who wants to verify
    the harness works has no guide to follow.

    T01 produced the naming taxonomy. T02 applied it (renaming testing-verification to
    testing-final-verification in the python3 manifest). This task documents how to exercise
    the full dispatch pipeline using real commands against the repo.

    Architecture spec: plan/architect-development-harness-phase1.md (Deliverable 3 section)
    Codebase analysis: plan/codebase/DEVELOPMENT-HARNESS-ARCHITECTURE.md (Manifest Utility Scripts section)

    ## Objective

    Produce an executable guide that a contributor can follow to validate manifest discovery,
    role resolution, and dispatch prompt assembly using real uv run commands with expected output.

    ## Inputs

    - Architecture spec Deliverable 3: plan/architect-development-harness-phase1.md
      (contains exact command templates, expected JSON output structures, and section descriptions
      for all 6 guide sections)
    - Codebase analysis Manifest Utility Scripts section:
      plan/codebase/DEVELOPMENT-HARNESS-ARCHITECTURE.md
    - Script files (read to verify CLI arguments and output format):
      - plugins/development-harness/scripts/manifest_discovery.py
      - plugins/development-harness/scripts/manifest_resolver.py
      - plugins/development-harness/scripts/dispatch_helper.py
    - Updated python3 manifest (T02 output):
      plugins/python3-development/manifests/python3/language-manifest.yaml
      (must reflect testing-final-verification in expected output examples)

    ## Requirements

    ### Section 1: Prerequisites

    1. List prerequisites: uv v0.10.0+, repo cloned with plugins, Python project with
       pyproject.toml (the repo itself qualifies)

    ### Section 2: Discover the Manifest

    2. Provide the uv run command to invoke manifest_discovery.py with --project-dir and --format json
    3. Show expected JSON output structure (selected manifest name, path, priority, candidates)
    4. IMPORTANT: Read manifest_discovery.py to verify the actual CLI argument names.
       The architecture spec suggests --project-dir and --format but these must be verified
       against the actual script. If arguments differ, use the actual argument names.
    5. Explain what to verify in the output

    ### Section 3: Resolve Roles

    6. Provide the uv run command to invoke manifest_resolver.py with the manifest path
    7. Show expected JSON output (roles, quality_gates, stage_skills)
    8. IMPORTANT: Read manifest_resolver.py to verify actual CLI argument names
    9. The stage_skills output must show testing-final-verification (not testing-verification)

    ### Section 4: Build the Dispatch Prompt

    10. Provide the uv run command to invoke dispatch_helper.py with manifest path,
        stage name, task file, and output artifact path
    11. IMPORTANT: Read dispatch_helper.py to verify actual CLI argument names
    12. Show expected dispatch prompt output structure (5 inputs assembled)
    13. Document the stage-to-key resolution logic (--stage planning resolves to
        which stage_skills key)

    ### Section 5: Dispatch the Generic Stage Agent

    14. Show the agent invocation pattern (this is descriptive, not runnable -- requires
        a Claude Code session)
    15. List what the agent would do with each of the 5 inputs

    ### Section 6: Expected Output

    16. Describe the artifact the agent would produce (.planning/harness/PLAN.md)
    17. List the expected content sections (architecture, acceptance tests, risk assessment,
        task skeletons, RT-ICA gate result)

    ## Constraints

    - ALL commands in Sections 2-4 MUST be verified against actual script argument parsers
      before writing the guide. Do not copy commands from the architecture spec without
      verifying they match the actual scripts.
    - Expected output JSON must reflect the UPDATED manifest (testing-final-verification)
    - The guide is user-facing documentation, not agent-facing. Write for a human contributor.
    - The docs/ directory may not exist yet -- create it if needed
    - All code fences must have language specifiers
    - All file references must use [text](./relative/path) markdown link syntax
    - Relative paths are from the docs/ directory
    - Use /dh: namespace (not /development-harness:) in any skill/agent activation references

    ## Expected Outputs

    - plugins/development-harness/docs/poc-validation-guide.md

    ## Acceptance Criteria

    1. File exists at plugins/development-harness/docs/poc-validation-guide.md
    2. Contains 6 sections matching the architecture spec structure
    3. Section 2 command uses actual CLI arguments from manifest_discovery.py (verified by reading the script)
    4. Section 3 command uses actual CLI arguments from manifest_resolver.py (verified by reading the script)
    5. Section 4 command uses actual CLI arguments from dispatch_helper.py (verified by reading the script)
    6. All expected output blocks use testing-final-verification (not testing-verification)
    7. The worked example uses the python3 manifest and planning stage
    8. All code fences have language specifiers
    9. All file references use markdown link syntax
    10. All skill/agent references use /dh: namespace

    ## Verification Steps

    1. Read the file and confirm all 6 sections exist
    2. Extract the manifest_discovery.py command from the guide and compare its arguments
       against the script's actual argparse/typer definition
    3. Extract the manifest_resolver.py command and compare similarly
    4. Extract the dispatch_helper.py command and compare similarly
    5. Grep for old key name in the guide:
       grep "testing-verification" plugins/development-harness/docs/poc-validation-guide.md
       (must return zero results for the bare key -- only testing-final-verification should appear)
    6. Grep for old namespace:
       grep "development-harness:" plugins/development-harness/docs/poc-validation-guide.md
       (must return zero results)
    7. Grep for code fences without language specifiers:
       grep -Pn '^```$' plugins/development-harness/docs/poc-validation-guide.md
       (must return no results)
    8. Run linting:
       uv run prek run --files plugins/development-harness/docs/poc-validation-guide.md

    ## CoVe Checks

    - Key claims to verify:
      - manifest_discovery.py accepts --project-dir and --format arguments
      - manifest_resolver.py accepts --manifest and --format arguments
      - dispatch_helper.py accepts --manifest, --stage, --task-file, --output-artifact arguments
    - Verification questions:
      1. What are the actual CLI argument names in manifest_discovery.py's argument parser?
      2. What are the actual CLI argument names in manifest_resolver.py's argument parser?
      3. What are the actual CLI argument names in dispatch_helper.py's argument parser?
    - Evidence to collect:
      - Read the argparse/typer setup in each script (first 50-80 lines typically)
      - Record exact argument names and any required vs optional distinction
    - Revision rule:
      - If any argument name in the guide does not match the script, replace with the
        actual argument name and update the expected output accordingly
- task: T04
  title: Copy python3-development agents to dh and apply dh namespace
    adaptations
  status: in-progress
  agent: general-purpose
  dependencies:
  - T00
  priority: 2
  complexity: medium
  accuracy-risk: low
  skills: []
  parallelize-with:
  - T01
  reason: Depends only on T00 (namespace rename). Runs in parallel with T01
    since they touch different files.
  handoff: All counterpart agents synced from python3-development with dh
    namespace applied. generic-stage-agent unchanged.
  body: |-
    ## Context

    The development-harness plugin (namespace: dh after T00) has agent files that were originally
    copied from python3-development but have drifted out of sync. The python3-development versions
    are the authoritative baseline for shared agents. This task re-syncs them using a COPY THEN
    PATCH strategy to minimize hallucination risk from read-compare-merge approaches.

    ## Objective

    Each dh agent that has a counterpart in python3-development reflects the p3d version as its
    baseline with dh-specific namespace adaptations applied. Agents unique to dh are untouched.

    ## Inputs

    - Source agents: plugins/python3-development/agents/*.md
    - Target agents: plugins/development-harness/agents/*.md

    ## Requirements

    ### Strategy: COPY THEN PATCH

    For each counterpart pair listed below:

    1. Read the python3-development version (source of truth)
    2. Read the current dh version (to identify any dh-specific content)
    3. Copy the python3-development version over the dh version as the new baseline
    4. After copying, apply these namespace adaptations to the copied file:
       a. Replace all `python3-development:` namespace references with `dh:`
       b. Replace all `@python3-development:` references with `@dh:`
       c. Replace all `/python3-development:` references with `/dh:`
    5. If the dh version had content that does NOT exist in the python3-development version
       (dh-specific additions), preserve that content by appending or merging it back.
       Use git diff or side-by-side comparison to identify dh-only content.

    ### Counterpart pairs to sync

    These agents exist in BOTH plugins/python3-development/agents/ AND plugins/development-harness/agents/:

    6. feature-researcher.md
    7. codebase-analyzer.md
    8. context-gathering.md
    9. context-refinement.md
    10. plan-validator.md
    11. feature-verifier.md
    12. integration-checker.md
    13. doc-drift-auditor.md
    14. ecosystem-researcher.md
    15. swarm-task-planner.md

    ### Agents to leave untouched

    16. generic-stage-agent.md -- exists only in dh, not in python3-development. Do NOT modify.
    17. service-docs-maintainer.md -- exists only in dh, not in python3-development. Do NOT modify.

    ### Agents NOT to copy (out of scope)

    18. These agents exist in python3-development but NOT in dh. Do NOT create them in dh:
        - t0-baseline-capture.md
        - tn-verification-gate.md
        - python-code-reviewer.md
        - python-pytest-architect.md
        - code-reviewer.md
        - python-cli-architect.md
        - python-cli-design-spec.md
        - semantic-code-search.md
        - ecosystem-researcher-v1.1-rt-ica.md

    ## Constraints

    - Do NOT modify any files in plugins/python3-development/ -- read only
    - Do NOT create new agent files in plugins/development-harness/agents/
    - Do NOT modify generic-stage-agent.md or service-docs-maintainer.md
    - Do NOT change any filesystem paths or directory references in agent files
      (e.g., references to plugins/development-harness/skills/... stay as-is)
    - Namespace replacement is text-level only: /python3-development: -> /dh:,
      @python3-development: -> @dh:, python3-development: -> dh: (in skill references)

    ## Expected Outputs

    - plugins/development-harness/agents/feature-researcher.md (modified)
    - plugins/development-harness/agents/codebase-analyzer.md (modified)
    - plugins/development-harness/agents/context-gathering.md (modified)
    - plugins/development-harness/agents/context-refinement.md (modified)
    - plugins/development-harness/agents/plan-validator.md (modified)
    - plugins/development-harness/agents/feature-verifier.md (modified)
    - plugins/development-harness/agents/integration-checker.md (modified)
    - plugins/development-harness/agents/doc-drift-auditor.md (modified)
    - plugins/development-harness/agents/ecosystem-researcher.md (modified)
    - plugins/development-harness/agents/swarm-task-planner.md (modified)

    ## Acceptance Criteria

    1. Each of the 10 counterpart agent files in dh reflects the python3-development version
       as its baseline content
    2. All python3-development: namespace references in those 10 files are replaced with dh:
    3. grep -r "python3-development:" plugins/development-harness/agents/ returns zero matches
       across the 10 counterpart files
    4. generic-stage-agent.md is byte-identical to its state before this task
    5. service-docs-maintainer.md is byte-identical to its state before this task
    6. No new files were created in plugins/development-harness/agents/
    7. No files in plugins/python3-development/ were modified
    8. git diff shows substantive content updates in the 10 files (not just whitespace)

    ## Verification Steps

    1. For each of the 10 counterpart files, diff the dh version against the p3d version:
       diff <(sed 's/python3-development:/dh:/g' plugins/python3-development/agents/feature-researcher.md) plugins/development-harness/agents/feature-researcher.md
       Differences should only be dh-specific additions (if any), not missing p3d content.
       Repeat for all 10 files.
    2. Grep for old namespace in counterpart files:
       grep -rn "python3-development:" plugins/development-harness/agents/feature-researcher.md plugins/development-harness/agents/codebase-analyzer.md plugins/development-harness/agents/context-gathering.md plugins/development-harness/agents/context-refinement.md plugins/development-harness/agents/plan-validator.md plugins/development-harness/agents/feature-verifier.md plugins/development-harness/agents/integration-checker.md plugins/development-harness/agents/doc-drift-auditor.md plugins/development-harness/agents/ecosystem-researcher.md plugins/development-harness/agents/swarm-task-planner.md
       (must return zero results)
    3. Verify untouched files:
       git diff plugins/development-harness/agents/generic-stage-agent.md | wc -l
       (must return 0)
       git diff plugins/development-harness/agents/service-docs-maintainer.md | wc -l
       (must return 0)
    4. Verify no new files:
       ls plugins/development-harness/agents/*.md | wc -l
       (must return 12 -- same count as before)
    5. Verify no p3d modifications:
       git diff --name-only plugins/python3-development/ | wc -l
       (must return 0)
    6. Run linting on a sample of modified files:
       uv run prek run --files plugins/development-harness/agents/feature-researcher.md
       uv run prek run --files plugins/development-harness/agents/swarm-task-planner.md
  started: '2026-03-18T22:53:32.675000+00:00'

---
