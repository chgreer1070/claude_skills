[project]
name = "fastmcp-creator-server"
version = "3.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.0,<3.2",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
strict = true
