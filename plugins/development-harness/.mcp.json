{
  "mcpServers": {
    "backlog": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "python",
        "-m",
        "backlog_core.server",
        "--project-dir",
        "${CLAUDE_PROJECT_DIR}"
      ]
    }
  }
}
